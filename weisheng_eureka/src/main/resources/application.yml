server: 
  port: 6868
  servlet: 
    context-path: /
spring: 
  application:  
    name: weisheng-eureka #指定服务名
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/ # 表示去这个url注册或者拿取